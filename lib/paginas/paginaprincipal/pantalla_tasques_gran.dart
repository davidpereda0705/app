import 'package:flutter/material.dart';

class PantallaTasquesGran extends StatelessWidget {
  const PantallaTasquesGran({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Pantalla de tasques gran (escriptori)'),
      ),
    );
  }
}